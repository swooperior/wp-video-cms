*ToDo;

Add refund action to video purchases area

Mobile styling

Style video purchases for better readability

Limit number of records on video purchases page









---------------
Code for adding video viewing capability to specific user:

    //First get current user ID
    $current_user = wp_get_current_user();
	$current_user_id = $current_user->ID;

    //Then create user class, and assign the video ID viewing capability to the user.
	$user = new WP_User($current_user_id);
	$user->add_cap('rw_vidyas_'.$post->ID,true);
	//or
	$user->remove_cap('rw_vidyas_'.$post->ID,true);




    





